/**
 * Blueprints de exámenes.
 * Define la distribución de preguntas por curso para cada tipo de examen.
 */
import data from './exam_blueprints.json' with { type: 'json' };

export const EXAM_BLUEPRINTS = data;
